import React from 'react';
import {Loading, LoadingWrapper} from './Loader.style';

const Loader = () => {
    return (
        <LoadingWrapper>
            <Loading/>
        </LoadingWrapper>
    );
};

export default Loader;
